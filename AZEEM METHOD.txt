  // AZEEM METHOD?
  
  function getNum(){ return Math.floor(Math.random()*244);}
const color = `rgb(${getNum()} ${getNum()} ${getNum()})`
document.querySelectorAll('.box').forEach(e => {
  e.style.backgroundColor= `rgb(${getNum()} ${getNum()} ${getNum()})`
  // e.style.backgroundColor= color
});